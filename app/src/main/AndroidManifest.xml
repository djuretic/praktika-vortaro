<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.esperantajvortaroj.app">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="com.esperantajvortaroj.app.SearchActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="com.esperantajvortaroj.app.DefinitionActivity"
            android:parentActivityName="com.esperantajvortaroj.app.SearchActivity" >
            <!-- Parent activity meta-data to support 4.0 and lower -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.esperantajvortaroj.app.SearchActivity" />
        </activity>
        <activity android:name="com.esperantajvortaroj.app.SettingsActivity"></activity>
        <activity android:name="com.esperantajvortaroj.app.SelectTranslationLanguageActivity">
            android:parentActivityName="com.esperantajvortaroj.app.SearchActivity" >
            <!-- Parent activity meta-data to support 4.0 and lower -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="com.esperantajvortaroj.app.SearchActivity" />
        </activity>
    </application>

</manifest>